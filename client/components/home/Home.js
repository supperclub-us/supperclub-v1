import React from "react";
import { useSelector } from "react-redux";
import { Header } from "../index";

/**
 * COMPONENT
 */
const Home = (props) => {

  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
